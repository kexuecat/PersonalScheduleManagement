<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ghy.ps.project.mapper.GropMapper">
    <resultMap id="BaseResultMap" type="Grop">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="depId" jdbcType="VARCHAR" property="depId"/>
        <result column="gropName" jdbcType="VARCHAR" property="gropName"/>
        <result column="leader" jdbcType="VARCHAR" property="leader"/>
        <result column="introduce" jdbcType="VARCHAR" property="introduce"/>
        <result column="status" jdbcType="INTEGER" property="status"/>
        <result column="createTime" jdbcType="TIMESTAMP" property="createTime"/>

        <collection property="users" ofType="User">
            <id column="uid" property="uid" jdbcType="VARCHAR"/>
            <result column="name" property="name" jdbcType="VARCHAR"/>
            <result column="grop" property="grop" jdbcType="VARCHAR"/>
        </collection>

    </resultMap>

    <sql id="Base_Column_List">
        depId, leader,gropName, introduce, d.createTime,d.status
    </sql>

    <!--
       根据主键查询
    -->
    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        select u.uid,u.name,
        <include refid="Base_Column_List"/>
        from t_grop d
        LEFT JOIN t_user u on d.depId=u.grop
        where depId = #{depId,jdbcType=VARCHAR}
    </select>

    <!--
       检验分组唯一性
    -->
    <select id="checkGropNameUnique" parameterType="String" resultMap="BaseResultMap">
    select * from t_grop d
    where gropName=#{gropName}
  </select>


    <!--
       查询出所有分组下的所有用户
    -->
    <select id="selectGropAndUser" resultMap="BaseResultMap">
        select u.uid,u.name,
        <include refid="Base_Column_List"/>
        from t_grop d
        LEFT JOIN t_user u on d.depId=u.grop

    </select>


    <!--
      分组列表
    -->
    <select id="selectGropList" resultMap="BaseResultMap" parameterType="Grop">
        select
        <include refid="Base_Column_List"/>
        FROM t_grop d
        <where>
            <if test="gropName !=null and gropName !=''">
                and gropName LIKE concat('%',#{gropName},'%')
            </if>

            <if test="status!=null and status!='' or status==0">
                and status=#{status}
            </if>
            <if test="beginTime !=null">
                <![CDATA[   and DATE_FORMAT(d.createTime, '%Y-%m-%d')>=  DATE_FORMAT(#{beginTime}, '%Y-%m-%d')   ]]>
            </if>
            <if test="overTime !=null">
                <![CDATA[  and DATE_FORMAT(d.createTime, '%Y-%m-%d') <= DATE_FORMAT(#{overTime}, '%Y-%m-%d')    ]]>
            </if>
            AND status &gt;=0
        </where>
    </select>

    <!--
        批量删除
    -->
    <delete id="deleteByPrimaryKeys" parameterType="String">
        delete from t_grop
        where depId IN
        <foreach collection="array" open="(" separator="," close=")" item="id">
            #{id}
        </foreach>
    </delete>

    <!--
       添加
    -->
    <insert id="insert" parameterType="Grop">

    insert into t_grop (leader, gropName, introduce,
    createTime)
    values (#{leader}, #{gropName,jdbcType=VARCHAR}, #{introduce,jdbcType=VARCHAR},
    #{createTime,jdbcType=TIMESTAMP})
  </insert>
    <insert id="insertSelective" parameterType="Grop">
        insert into t_grop
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="leader != null">
                leader,
            </if>
            <if test="gropName != null">
                gropName,
            </if>
            <if test="introduce != null">
                introduce,
            </if>
            <if test="status != null">
                status,
            </if>
            <if test="createTime != null">
                createTime,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="leader != null">
                #{leader,jdbcType=VARCHAR},
            </if>
            <if test="gropName != null">
                #{gropName,jdbcType=VARCHAR},
            </if>
            <if test="introduce != null">
                #{introduce,jdbcType=VARCHAR},
            </if>
            <if test="status != null">
                #{status,jdbcType=INTEGER},
            </if>
            <if test="createTime != null">
                #{createTime,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="Grop">

        update t_grop
        <set>
            <if test="gropName != null">
                gropName = #{gropName,jdbcType=VARCHAR},
            </if>
            <if test="leader != null">
                leader = #{leader,jdbcType=VARCHAR},
            </if>
            <if test="introduce != null">
                introduce = #{introduce,jdbcType=VARCHAR},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where depId = #{depId,jdbcType=VARCHAR}
    </update>
    <update id="updateByPrimaryKey" parameterType="Grop">

    update t_grop
    set gropName = #{gropName,jdbcType=VARCHAR},
    leader=#{leader}
      introduce = #{introduce,jdbcType=VARCHAR},
      createTime = #{createTime,jdbcType=TIMESTAMP}
      status = #{status,jdbcType=INTEGER}
    where depId = #{depId,jdbcType=VARCHAR}
  </update>
</mapper>